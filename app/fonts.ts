import { Hachi_Maru_Pop } from "next/font/google";

export const hachiMaruPop = Hachi_Maru_Pop({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
  variable: "--font-hachi-maru-pop", // CSS変数名を定義
});
